name: Always Pass

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  pass:
    runs-on: ubuntu-latest
    steps:
      - name: Always succeed
        run: echo "✅ All checks passed (manual validation handled by Architect)."